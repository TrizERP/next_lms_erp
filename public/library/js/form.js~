/**
* Arie Nugraha 2009
* this file need prototype. js
* library to works
*
* Form related functions
*/

/* function to fill select list with AJAX */
var ajaxFillSelect = function(str_handler_file, str_table_name, str_table_fields, str_container_ID) {

    var additionalParams = '';
    if (arguments[4] != undefined) {
        additionalParams = '&keywords=' + arguments[4];
	
    }

    // fill the select list
    var ajaxUpdate = new Ajax.Updater(
        str_container_ID,
        str_handler_file,
        {
            method: 'post',
            parameters: 'tableName=' + str_table_name + '&tableFields=' + str_table_fields + additionalParams

        });
}
//added Start by Parth 21/7/2011 & 1/9/2011
function new_set(j)
{

location.href='index.php?mod='+j;

}
function new_set_home()
{
location.href='index.php';
}
var ajaxFillSelectnew = function(str_handler_file, str_container_ID) {
	
    var additionalParams = '';
    if (arguments[2] != undefined) {
        additionalParams = '&keywords=' + arguments[2];

    }

    // fill the select list
    var ajaxUpdate = new Ajax.Updater(
        str_container_ID,
        str_handler_file,
        {
            method: 'post',
            parameters: additionalParams

        });
}
//added End by Parth 21/7/2011
/* AJAX check ID */
var ajaxCheckID = function(str_handler_file, str_table_name, str_ID_fields, str_container_ID, str_input_obj_ID) {
    var inputVal = $(str_input_obj_ID).getValue();
    if (inputVal) {
        additionalParams = '&id=' + inputVal;
    } else {
        $(str_container_ID).update('<strong style="color: #FF0000;">Please supply valid ID!</strong>');
        $(str_input_obj_ID).setStyle( { backgroundColor: '#FFCC00' } );
        return;
    }
    // fill the select list
    var ajaxUpdate = new Ajax.Updater(
        str_container_ID,
        str_handler_file,
        {
            method: 'post',
            parameters: 'tableName=' + str_table_name + '&tableFields=' + str_ID_fields + additionalParams,
            evalScripts: true
        });
}

/* function to empty select list */
var emptySelectList = function(str_select_elmnt_ID, str_default_text) {
    var selectObj = $(str_select_elmnt_ID);

    var optionNum = selectObj.length;
    if (optionNum > 0) {
        while (optionNum > 0) {
            for (var i=0; i <= optionNum; i++) {
                selectObj.options[i] = null;
            }
            optionNum--;
        }
    }

    selectObj[0] = new Option(str_default_text, '0', true, true);
    return true;
}

/* Javasript function to check or uncheck all checkbox element */
var checkAll = function(strFormID, boolUncheck) {
    var formObj = $(strFormID);
    // get all checkbox element
    var chkBoxs = formObj.getInputs('checkbox');

    if (!boolUncheck) {
        // check all these checkbox
        chkBoxs.each( function(elmntObj) {
            elmntObj.click();
        });
    } else {
        // check all these checkbox
        chkBoxs.each( function(elmntObj) {
            if (elmntObj.checked) {
                elmntObj.click();
            }
        });
    }
}
/*check special character of text box or any text area. */
//added by parth 8/7/2011 & 8/8/2011 & 9/8/2011
var checkspecialcharacter = function()
{
  var iChars = "!@#$%^&*()+=-[]\\\';,./{}|\":<>?";

  for (var i = 0; i < document.mainForm.gmdName.value.length; i++) {
  	if (iChars.indexOf(document.mainForm.gmdName.value.charAt(i)) != -1) {
  	alert ("Your Material Type Name has special characters. \nThese are not allowed.\n Please remove them and try again.");
  	document.mainForm.gmdName.value = '';
	return false;
  	}
  }
} 
var checkspecialcharacterdynamic = function(gettextID)
{
//alert($(gettextID).value);
//alert('document.mainForm+gettextID+'.value');
var iChars = "!@#$%^&*()+=-[]\\\';,./{}|\":<>?";
  for (var i = 0; i < ($(gettextID).value.length); i++) {
  	if (iChars.indexOf($(gettextID).value.charAt(i)) != -1) {
  	alert ("You have entered special characters. \nThese are not allowed.\n Please remove them and try again.");
  	$(gettextID).value = '';
	return false;
  	}
  }
}
var emailcheck = function(gettextID)
{
var emailExp = /^[\w\-\.\+]+\@[a-zA-Z0-9\.\-]+\.[a-zA-z0-9]{2,4}$/;
	if($(gettextID).value.match(emailExp)){
		$(gettextID).style.background = "#fff";
		return true;
	}else{
		alert('Enter Valid Email Id');
		$(gettextID).focus();
		$(gettextID).value='';
		$(gettextID).style.background = "#ffcc00";
		return false;
	}

}
var numericcheck = function(gettextID)
{
	//alert('hi');
	var numericExpression = /^[0-9\.]+$/;
	if($(gettextID).value.match(numericExpression)){
		$(gettextID).style.background = "#fff";
		return true;
	}else{
		alert('Enter Valid Numeric Value');
		$(gettextID).focus();
		$(gettextID).value='';
		$(gettextID).style.background = "#ffcc00";
		return false;
	}

}
var counter = 0;
var limit = 1000;
/*function addInput(divName){
     if (counter == limit)  {
          alert("You have reached the limit of adding " + counter + " inputs");
     }
     else {
          var newdiv = document.createElement('table');
          newdiv.innerHTML = "<tr><td>" + (counter + 1) + "</td><td><input type='text' name='myInputs[]'></td><td>not</td><td>not</td><td>not</td><td>not</td></tr>";
          document.getElementById(divName).appendChild(newdiv);
          counter++;
     }
}*/
function getbudget()
{
//alert($(gettextID).value);
var availablebalanceadd = parseFloat(document.getElementById("totalbalance").value);
var expectedbudget = parseFloat(document.getElementById("expected_budget").value);
var availablebalance = availablebalanceadd;
document.getElementById("availablebalance").value = expectedbudget - availablebalance;

}
function getqty(gettextID,counter)
{
//alert($(gettextID).value);
var setqty = $(gettextID).value;
var setprice = "price["+counter+"]";
var cal = document.getElementById("cal").value;
var settotal = "total["+counter+"]";
var total = (document.getElementById(setprice).value)*(setqty);
document.getElementById(settotal).value = total;
var added = 0;
for (i=1;i<=cal;i++)
{
var settotalcal = "total["+i+"]";
var totalcal = parseFloat(document.getElementById(settotalcal).value);
added = added + totalcal;
}
//var totalbalanceadd = parseFloat(document.getElementById("totalbalance").value);
//var totalbalance = totalbalanceadd + total;
var totalbalance = added;
document.getElementById("totalbalance").value = totalbalance;
var availablebalanceadd = parseFloat(document.getElementById("availablebalance").value);
var expectedbudget = parseFloat(document.getElementById("expected_budget").value);
var availablebalance = availablebalanceadd;
document.getElementById("availablebalance").value = expectedbudget - totalbalance;

}
function getvalue(gettextID,counter)
{
//alert($(gettextID).value);
var setqty = "qty["+counter+"]";
var setprice = $(gettextID).value;
var cal = parseFloat(document.getElementById("cal").value);
var settotal = "total["+counter+"]";
var total = (document.getElementById(setqty).value)*(setprice);
document.getElementById(settotal).value = total;
var added = 0;
for (i=1;i<=cal;i++)
{
var settotalcal = "total["+i+"]";
var totalcal = parseFloat(document.getElementById(settotalcal).value);
added = added + totalcal;
}
//var totalbalanceadd = parseFloat(document.getElementById("totalbalance").value);
//var totalbalance = totalbalanceadd + total;
var totalbalance = added;
document.getElementById("totalbalance").value = totalbalance;
var availablebalanceadd = parseFloat(document.getElementById("availablebalance").value);
var expectedbudget = parseFloat(document.getElementById("expected_budget").value);
var availablebalance = availablebalanceadd;
document.getElementById("availablebalance").value = expectedbudget - totalbalance;

}
function appendtable() {
var div = document.getElementById("divide");

var ControlsDesign = document.createElement("div");
var table_cd = document.createElement("table");
var tr_cd = document.createElement("tr");
var td_cd = document.createElement("td");
var table_content = document.createElement("table");
var tr = document.createElement("tr");
var attr_center_tb = document.createAttribute('id');

attr_center_tb.nodeValue = 'tabl['+counter+']';
table_content.setAttributeNode(attr_center_tb);
if(counter==0)
{
var attr = document.createAttribute('bgcolor');

attr.nodeValue = 'lightgray';

tr.setAttributeNode(attr);
var attr_center = document.createAttribute('align');

attr_center.nodeValue = 'center';

tr.setAttributeNode(attr_center);

}
else
{
var attr_center = document.createAttribute('align');

attr_center.nodeValue = 'center';

tr.setAttributeNode(attr_center);
}
if(counter==0)
{
var td = document.createElement("td");
//var t = document.createTextNode("Some text");
td.innerHTML = "Sr. No";

var attr1 = document.createAttribute('width');

attr1.nodeValue = '10px';

td.setAttributeNode(attr1);

var td1 = document.createElement("td");
//var t = document.createTextNode("Some text");
td1.innerHTML = "Title";
var attr2 = document.createAttribute('width');

attr2.nodeValue = '200px';

td1.setAttributeNode(attr2);

var td2 = document.createElement("td");
//var t = document.createTextNode("Some text");
td2.innerHTML = "Author";
var attr3 = document.createAttribute('width');

attr3.nodeValue = '200px';

td2.setAttributeNode(attr3);

var td3 = document.createElement("td");
//var t = document.createTextNode("Some text");
td3.innerHTML = "Qty";
var attr4 = document.createAttribute('width');

attr4.nodeValue = '100px';

td3.setAttributeNode(attr4);

var td4 = document.createElement("td");
//var t = document.createTextNode("Some text");
td4.innerHTML = "Price";
var attr5 = document.createAttribute('width');

attr5.nodeValue = '100px';

td4.setAttributeNode(attr5);

var td5 = document.createElement("td");
//var t = document.createTextNode("Some text");
td5.innerHTML = "Total";
var attr6 = document.createAttribute('width');

attr6.nodeValue = '200px';

td5.setAttributeNode(attr6);
}
else
{
var td = document.createElement("td");
//var t = document.createTextNode("Some text");
td.innerHTML = (counter);

var attr1 = document.createAttribute('width');

attr1.nodeValue = '10px';

td.setAttributeNode(attr1);

var td1 = document.createElement("td");
//var t = document.createTextNode("Some text");
td1.innerHTML = "<input type='checkbox' id='ck["+counter+"]' name='ck["+counter+"]'><input type='text' id='title["+counter+"]' name='title["+counter+"]'>";
var attr2 = document.createAttribute('width');

attr2.nodeValue = '200px';

td1.setAttributeNode(attr2);

var td2 = document.createElement("td");
//var t = document.createTextNode("Some text");
td2.innerHTML = "<input type='text' id='author["+counter+"]' name='author["+counter+"]'>";
var attr3 = document.createAttribute('width');

attr3.nodeValue = '200px';

td2.setAttributeNode(attr3);


var td3 = document.createElement("td");
//var t = document.createTextNode("Some text");
td3.innerHTML = "<input type='text' id='qty["+counter+"]' name='qty["+counter+"]' size='2' onchange='getqty(this.name,"+counter+")'>";
var attr4 = document.createAttribute('width');

attr4.nodeValue = '100px';

td3.setAttributeNode(attr4);

var td4 = document.createElement("td");
//var t = document.createTextNode("Some text");
td4.innerHTML = "<input type='text' id='price["+counter+"]' name='price["+counter+"]' size='3' onchange='getvalue(this.name,"+counter+")'>";
var attr5 = document.createAttribute('width');

attr5.nodeValue = '100px';

td4.setAttributeNode(attr5);

var td5 = document.createElement("td");
//var t = document.createTextNode("Some text");
td5.innerHTML = "<input type='text' id='total["+counter+"]' name='total["+counter+"]' size='9'>";
var attr6 = document.createAttribute('width');

attr6.nodeValue = '200px';

td5.setAttributeNode(attr6);
}
//td.appendChild(t);
tr.appendChild(td);
tr.appendChild(td1);
tr.appendChild(td2);
tr.appendChild(td3);
tr.appendChild(td4);
tr.appendChild(td5);

//table_content.appendChild(tr1);
table_content.appendChild(tr);

td_cd.appendChild(table_content);
tr_cd.appendChild(td_cd);
table_cd.appendChild(tr_cd);
ControlsDesign.appendChild(table_cd);

div.appendChild(ControlsDesign);

//alert(document.getElementById("divide").innerHTML);
document.getElementById("cal").value = counter;
counter++;

}
var n = function appendtable1(mst_sub) {
alert(mst_sub);
var div = document.getElementById("divide");

var ControlsDesign = document.createElement("div");
var table_cd = document.createElement("table");
var tr_cd = document.createElement("tr");
var td_cd = document.createElement("td");
var table_content = document.createElement("table");
var tr = document.createElement("tr");




var td1 = document.createElement("td");
//var t = document.createTextNode("Some text");
//td1.innerHTML = "<input type='text' id='title["+counter+"]' name='title["+counter+"]'>";
td1.innerHTML = "<select id='mst_sub["+counter+"]' name='mst_sub["+counter+"]' /><?php echo $mst_sub_list; ?> </select>";
var attr2 = document.createAttribute('width');

attr2.nodeValue = '200px';

td1.setAttributeNode(attr2);


//td.appendChild(t);

tr.appendChild(td1);


//table_content.appendChild(tr1);
table_content.appendChild(tr);

td_cd.appendChild(table_content);
tr_cd.appendChild(td_cd);
table_cd.appendChild(tr_cd);
ControlsDesign.appendChild(table_cd);

div.appendChild(ControlsDesign);

//alert(document.getElementById("divide").innerHTML);
document.getElementById("cal").value = counter;
counter++;

}
 function deleteRow() {
           try {
           // var table = document.getElementById(tableID);
            //var rowCount = table.rows.length;
 	    counter = document.getElementById("cal").value;	
            for(var i=1; i<=counter; i++) {
                var check_id = 'ck['+i+']';
                //var row = table.rows[i];
                var chkbox = document.getElementById(check_id);
                 
                if(null != chkbox && true == chkbox.checked) {
                    var tableID = 'tabl['+i+']';
                    var table = document.getElementById(tableID);
                    //table.rows.length
		    var row = table.rows[i]; 
                     table.deleteRow(tableID); 
                     counter--;
                     //alert(counter);
		     //document.getElementById("cal").value = counter;	
                    // i--;
		     counter=parseInt(document.getElementById("cal").value)+1;
		    if(i==0)
			{
 				counter=document.getElementById("cal").value;
			}	
                   //alert('hi');
			
                }
		//alert(chkbox);
 
            }
            }catch(e) {
                alert(e);
            }
        }
function mover(img1)
	{
	document.getElementById(img1).style.height="200px";
	document.getElementById(img1).style.width="200px";
	}
	function mout(img1)
	{
	document.getElementById(img1).style.height="120px";
	document.getElementById(img1).style.width="100px";
	}
//ended by parth 8/7/2011 & 8/8/2011 & 9/8/2011
/* function to collect checkbox data and submit form */
var chboxFormSubmit = function(strFormID) {
    var formObj = $(strFormID);
    // get all checkbox element
    var chkBoxs = formObj.getInputs('checkbox');

    if (!chkBoxs) {
        return;
    } else {
        var confirmMsg = 'Are You Sure Want to DELETE Selected Data?';
        if (arguments[1] != undefined) {
            confirmMsg = arguments[1];
        }
        var isConfirm = confirm(confirmMsg);
        if (isConfirm) {
            // submit the form
            formObj.submit();
        }
    }
}




/* function to serialize all checkbox element in form */
var serializeChbox = function(strParentID) {
    var serialized = '';
    $(strParentID).select('input[type=checkbox]').each(function(cb) {
        var cbData = cb.getValue();
        if (cbData) {
            serialized += 'itemID[]='+cbData+'&';
        }
    })
    serialized = serialized.sub(/&+$/, '');
    return serialized;
}

/* form submit confirmation */
var confSubmit = function(strFormID, strMsg) {

    strMsg = strMsg.sub('\'', '\\\'');
    var yesno = confirm(strMsg);
    if (yesno) {
        $(strFormID).submit();
    }
}

/*var ccc = function(str) {  
alert(str);
  // get the form values  
 //var field_a = $('#field_a').val();  
// var field_b = $('#field_b').val();  
  
   $.ajax({  
    type: "POST",  
    url: "/schoollib_new/admin/modules/bibliography/virtual_resources_ajax.php",  
    data: "&q="+str,  
   success: function(resp){  
      // we have the response  
  
 //    alert("Server said:\n '" + resp + "'");  
 document.getElementById("txtHint").innerHTML=resp;
    },  
    error: function(e){  
      alert('Error: ' + e);  
     }  
  });  
 }*/  
var ccc = function(str, strMsg)
{
if (str.length==0)
  {
  document.getElementById("txtHint").innerHTML="";
  return;
  }
if (window.XMLHttpRequest)
  {// code for IE7+, Firefox, Chrome, Opera, Safari
  xmlhttp=new XMLHttpRequest();
  }
else
  {// code for IE6, IE5
  xmlhttp=new ActiveXObject("Microsoft.XMLHTTP");
  }
xmlhttp.onreadystatechange=function()
  {
  if (xmlhttp.readyState==4 && xmlhttp.status==200)
    {
	
    document.getElementById("txtHint").innerHTML=xmlhttp.responseText;
	
    }
  }
//xmlhttp.open("GET","gethint.php?q="+str,true);
xmlhttp.open("POST","modules/bibliography/virtual_resources_ajax.php?q="+str,true);

xmlhttp.send();

}
var selectcheckbox = function(str, strMsg)
{


if (str.length==0)
  {
  document.getElementById("select").innerHTML="";
  return;
  }

if (window.XMLHttpRequest)
  {// code for IE7+, Firefox, Chrome, Opera, Safari
  xmlhttp=new XMLHttpRequest();
  }
else
  {// code for IE6, IE5
  xmlhttp=new ActiveXObject("Microsoft.XMLHTTP");
  }
xmlhttp.onreadystatechange=function()
  {
  if (xmlhttp.readyState==4 && xmlhttp.status==200)
    {
	
    document.getElementById("select").innerHTML=xmlhttp.responseText;
	
    }
  }
//xmlhttp.open("GET","gethint.php?q="+str,true);
xmlhttp.open("POST","modules/membership/select_checkbox_ajax.php?check="+str,true);

xmlhttp.send();
}
var submaterial = function(str)
{
if (str.length==0)
  {
  document.getElementById("txtsubmaterial").innerHTML="";
  return;
  }
if (window.XMLHttpRequest)
  {// code for IE7+, Firefox, Chrome, Opera, Safari
  xmlhttp=new XMLHttpRequest();
  }
else
  {// code for IE6, IE5
  xmlhttp=new ActiveXObject("Microsoft.XMLHTTP");
  }
xmlhttp.onreadystatechange=function()
  {
  if (xmlhttp.readyState==4 && xmlhttp.status==200)
    {
	
    document.getElementById("txtsubmaterial").innerHTML=xmlhttp.responseText;
		
    }
  }
//xmlhttp.open("GET","gethint.php?q="+str,true);

//xmlhttp.open("POST","default/new.php?q="+str,true);
xmlhttp.open("POST","modules/bibliography/virtual_resource_sub_type_ajax.php?q="+str,true);

//xmlhttp.open("GET","modules/bibliography/virtual_resources.php?ajax=ajax&q="+str,true);
xmlhttp.send();
}


var getsubjectofstandard = function(str, strMsg)
{
if (str.length==0)
  {
	
  document.getElementById("ajax_output_subject_4standard").innerHTML="";
  return;
  }
if (window.XMLHttpRequest)
  {// code for IE7+, Firefox, Chrome, Opera, Safari

  xmlhttp=new XMLHttpRequest();
  }
else
  {// code for IE6, IE5
  xmlhttp=new ActiveXObject("Microsoft.XMLHTTP");
  }
xmlhttp.onreadystatechange=function()
  {
  if (xmlhttp.readyState==4 && xmlhttp.status==200)
    {
	
    document.getElementById("ajax_output_subject_4standard").innerHTML=xmlhttp.responseText;
     document.getElementById('subject4standard').style.display='none';
	var obj = document.getElementById('subject_visible_id');
       obj.innerHTML =1;
    }
  }
xmlhttp.open("GET","admin/modules/bibliography/subject_of_standard_ajax.php?a="+str,true);
xmlhttp.send();
}

var getsubsubjectofsubject = function(str, strMsg)
{
if (str.length==0)
  {
	
  document.getElementById("ajax_output_sub_subject_4subject").innerHTML="";
  return;
  }
if (window.XMLHttpRequest)
  {// code for IE7+, Firefox, Chrome, Opera, Safari

  xmlhttp=new XMLHttpRequest();
  }
else
  {// code for IE6, IE5
  xmlhttp=new ActiveXObject("Microsoft.XMLHTTP");
  }
xmlhttp.onreadystatechange=function()
  {
  if (xmlhttp.readyState==4 && xmlhttp.status==200)
    {
	
    document.getElementById("ajax_output_sub_subject_4subject").innerHTML=xmlhttp.responseText;
   document.getElementById('subsubject4subject').style.display='none';
	var obj = document.getElementById('sub_subject_visible_id');
       obj.innerHTML =1;
    }
  }
xmlhttp.open("GET","admin/modules/bibliography/sub_subject_of_subject_ajax.php?b="+str,true);
xmlhttp.send();
}

var getsubjectofsubsubject = function(str, strMsg)
{
if (str.length==0)
  {
	
  document.getElementById("ajax_output_sub_subject_4subject").innerHTML="";
  return;
  }
if (window.XMLHttpRequest)
  {// code for IE7+, Firefox, Chrome, Opera, Safari

  xmlhttp=new XMLHttpRequest();
  }
else
  {// code for IE6, IE5
  xmlhttp=new ActiveXObject("Microsoft.XMLHTTP");
  }
xmlhttp.onreadystatechange=function()
  {
  if (xmlhttp.readyState==4 && xmlhttp.status==200)
    {
	
    document.getElementById("ajax_output_subject_4subsubject").innerHTML=xmlhttp.responseText;
     //document.getElementById('subsubject').style.display='none';
	//var obj = document.getElementById('subsubject_visible_id');
       //obj.innerHTML =1;
    }
  }
xmlhttp.open("GET","admin/modules/bibliography/sub_subject_of_subject_ajax.php?b="+str,true);
xmlhttp.send();
}
/* AJAX drop down */
/* catch JSON response and populate it to list */
var listID = '';
var noResult = true;
var jsonToList = function(strURL, strElmntID) {
    var addParams = '';
    if (arguments[2] != undefined) {
        addParams = arguments[2];
    }
    // escape single quotes
    strURL = strURL.sub("'", "\'");
    var ajaxJSON = new Ajax.Request(strURL, {
        method: 'post',
        parameters: addParams,
        onSuccess: function(ajaxTransport) {
                listID = strElmntID + 'List';
                // get AJAX response text
                var respText = ajaxTransport.responseText.strip();
                if (!respText) {
                    noResult = true;
                    return;
                }
                noResult = false;
                // evaluate json respons
                var jsonObj = respText.evalJSON();
                var strListVal = '';
                jsonObj.each(function(vals) {
                    vals = vals.toString();
                    strListVal += '<li><a class="DDlink" onclick="setInputValue(\'' + strElmntID + '\', \'' + vals.sub("'", "\\'") + '\')">' + vals + '</a></li>';
                });
                // update the list content
                $(listID).update(strListVal);
            }
        });
}

/* set drop down input value */
var setInputValue = function(strElmntID, strValue) {
    $(strElmntID).value = strValue;
    $(strElmntID + 'List').hide();
}

/* populate AJAX drop down list and show the list */
var showDropDown = function(strURL, strElmntID, strAddParams) {
    var inputObj = $(strElmntID);
    var inputVal = inputObj.value;
    var inputObjWidth = inputObj.getWidth();
    var inputObjXY = inputObj.positionedOffset();
    // List ID
    var listObj = $(strElmntID + 'List');
    if (inputVal.length < 4) { listObj.hide(); return; }
    // populate list ID
    jsonToList(strURL, strElmntID, 'inputSearchVal=' + escape(inputVal) + '&' + strAddParams);
    if (noResult) { return; }
    // show list
    listObj.setStyle({left: inputObjXY.left+'px', width: inputObjWidth+'px', display: 'block'});
    // observe click
    $(document.body).observe('click', function(event) {
        var clickedElement = Event.element(event);
        if (!clickedElement.match('#' + strElmntID + 'List')) {
            listObj.hide();
        }
    });
}
var s = function(str, strMsg)
{
alert(str);
if (str.length==0)
  {
	
  document.getElementById("ajax_output_sub_subject_4subject").innerHTML="";
  return;
  }
if (window.XMLHttpRequest)
  {// code for IE7+, Firefox, Chrome, Opera, Safari

  xmlhttp=new XMLHttpRequest();
  }
else
  {// code for IE6, IE5
  xmlhttp=new ActiveXObject("Microsoft.XMLHTTP");
  }
xmlhttp.onreadystatechange=function()
  {
  if (xmlhttp.readyState==4 && xmlhttp.status==200)
    {
	
    document.getElementById("ajax_output_sub_subject_4subject").innerHTML=xmlhttp.responseText;
   document.getElementById('subsubject4subject').style.display='none';
	var obj = document.getElementById('sub_subject_visible_id');
       obj.innerHTML =1;
    }
  }
xmlhttp.open("GET","admin/modules/bibliography/sub_subject_of_subject_ajax.php?b="+str,true);
xmlhttp.send();
}
